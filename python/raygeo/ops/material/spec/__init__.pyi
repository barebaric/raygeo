# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405
r"""
Fold input types: the stock, the entries, and the grid budget.
"""

import builtins
from raygeo import geo
import typing
__all__ = [
    "CylinderStock",
    "FoldEntry",
    "GridBudget",
    "GridSpec",
    "MaterialFoldSpec",
    "PrismaticStock",
]

@typing.final
class CylinderStock:
    r"""
    A cylindrical (rotary) stock, folded in unrolled space.
    
    The axial coordinate maps to world x in ``[0, length]``; the
    circumference (arc length) maps to world y in
    ``[-pi * diameter / 2, pi * diameter / 2]``.
    """
    @property
    def diameter(self) -> builtins.float:
        r"""
        Workpiece diameter in mm.
        """
    @property
    def length(self) -> builtins.float:
        r"""
        Axial length in mm.
        """
    def __new__(cls, diameter: builtins.float, length: builtins.float) -> CylinderStock: ...

@typing.final
class FoldEntry:
    r"""
    One compute node's contribution to a fold.
    """
    @property
    def source_key(self) -> builtins.str:
        r"""
        Node key of the source (for provenance).
        """
    @property
    def placement(self) -> geo.Matrix:
        r"""
        Workpiece-local to world-mm placement of the effects.
        """
    @property
    def effects(self) -> builtins.list[typing.Any]:
        r"""
        The effects this entry contributes.
        """
    def __new__(cls, source_key: builtins.str, placement: geo.Matrix, effects: typing.Sequence[typing.Any]) -> FoldEntry: ...

@typing.final
class GridBudget:
    r"""
    Resolution budget for stock-grid outputs.
    """
    @property
    def px_per_mm(self) -> builtins.float:
        r"""
        Requested grid density in pixels per millimetre.
        """
    @property
    def max_px(self) -> builtins.int:
        r"""
        Per-side pixel cap; ``px_per_mm`` is scaled down to fit.
        """
    def __new__(cls, px_per_mm: builtins.float = 50.0, max_px: builtins.int = 8192) -> GridBudget: ...

@typing.final
class GridSpec:
    r"""
    Grid placement of raster outputs in world mm.
    """
    @property
    def origin_mm(self) -> tuple[builtins.float, builtins.float]:
        r"""
        World-mm origin of the grid's (0, 0) pixel corner.
        """
    @property
    def px_per_mm(self) -> tuple[builtins.float, builtins.float]:
        r"""
        Grid density in pixels per millimetre ``(x, y)``.
        """
    @property
    def size_px(self) -> tuple[builtins.int, builtins.int]:
        r"""
        Grid size in pixels ``(width, height)``.
        """

@typing.final
class MaterialFoldSpec:
    r"""
    Full input to :func:`fold_effects`.
    """
    @property
    def stock(self) -> typing.Any:
        r"""
        The stock to fold against.
        """
    @property
    def entries(self) -> builtins.list[FoldEntry]:
        r"""
        Effect-bearing entries, in any order.
        """
    @property
    def grid(self) -> GridBudget:
        r"""
        Grid budget for raster outputs.
        """
    def __new__(cls, stock: typing.Any, entries: typing.Sequence[FoldEntry], grid: typing.Optional[GridBudget] = None) -> MaterialFoldSpec: ...

@typing.final
class PrismaticStock:
    r"""
    A prismatic stock: 2D outline extruded over a thickness.
    
    Z convention: top surface at ``z = 0``, bottom at
    ``z = -thickness``.
    """
    @property
    def polygons(self) -> builtins.list[builtins.list[tuple[builtins.float, builtins.float]]]:
        r"""
        Stock outline polygons in world mm.
        """
    @property
    def thickness(self) -> builtins.float:
        r"""
        Stock thickness in mm.
        """
    def __new__(cls, polygons: typing.Sequence[typing.Sequence[tuple[builtins.float, builtins.float]]], thickness: builtins.float) -> PrismaticStock: ...

