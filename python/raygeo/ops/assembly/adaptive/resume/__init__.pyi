# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405

import builtins
import collections.abc
import raygeo
from raygeo.geo.algo import medial_axis
import typing
__all__ = [
    "emit_resume_travel",
    "mat_resume_target",
    "nearest_uncleared_node",
    "smooth_travel_path",
    "try_resume",
]

def emit_resume_travel(ops: raygeo.ops.Ops, cleared: raygeo.ops.cut.cleared_area.ClearedArea, axis: raygeo.geo.algo.medial_axis.MedialAxis | None, from_pt: tuple[float, float], to_pt: tuple[float, float], pocket_boundary: collections.abc.Sequence[tuple[float, float]], islands: collections.abc.Sequence[collections.abc.Sequence[tuple[float, float]]] = [], radius: float = 3, cut_z: float = -5) -> None:
    r"""
    Emit a safe resume travel from *from_pt* to *to_pt* into *ops*.
    """

def mat_resume_target(axis: raygeo.geo.algo.medial_axis.MedialAxis, cleared: raygeo.ops.cut.cleared_area.ClearedArea, tool_pos: tuple[float, float], valid_tool_area: collections.abc.Sequence[collections.abc.Sequence[tuple[float, float]]]) -> tuple[list[tuple[float, float]], float] | None:
    r"""
    Pick a resume target by walking the MAT to the nearest uncleared node.
    """

def nearest_uncleared_node(axis: medial_axis.MedialAxis, start: builtins.int, is_cleared: typing.Sequence[builtins.bool]) -> typing.Optional[builtins.int]:
    r"""
    BFS over the Medial Axis tree from a start node, returning the index
    of the nearest (fewest hops) node that is **not** cleared.
    
    :param axis: ``MedialAxis`` instance.
    :param start: Starting node index.
    :param is_cleared: Cleared/uncleared mask (one bool per node).
    :returns: Index of the nearest uncleared node, or ``None``.
    """

def smooth_travel_path(from_pt: tuple[float, float], raw: collections.abc.Sequence[tuple[float, float]], islands: collections.abc.Sequence[collections.abc.Sequence[tuple[float, float]]] = [], remaining: collections.abc.Sequence[collections.abc.Sequence[tuple[float, float]]] = [], clearance: float = 1) -> list[tuple[float, float]]:
    r"""
    Smooth and shorten a cleared-territory travel path.
    """

def try_resume(cleared: raygeo.ops.cut.cleared_area.ClearedArea, ops: raygeo.ops.Ops, tool: raygeo.ops.assembly.adaptive.tool.Tool, pocket_boundary: collections.abc.Sequence[tuple[float, float]], islands: collections.abc.Sequence[collections.abc.Sequence[tuple[float, float]]] = [], radius: float = 3, step_length: float = 0.6, advance: float = 1.5, cut_z: float = -5, valid_tool_area: collections.abc.Sequence[collections.abc.Sequence[tuple[float, float]]] = [], axis: raygeo.geo.algo.medial_axis.MedialAxis | None = None, last_resume_area: float = -1) -> bool:
    r"""
    Try to recover after the tool stalls or is detected as stuck.
    """

