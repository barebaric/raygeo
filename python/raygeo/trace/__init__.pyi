# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405
r"""
Binary trace-file reader and shared move-type classification.

MoveKind — standard move-type classification shared by all operations.
TraceFile — read a .bin trace file with random access to records.
TraceRecord — one per-step record with dot-accessible fields.
"""

import builtins
import typing
__all__ = [
    "MoveKind",
    "ResumeSource",
    "RouteSource",
    "StepStatus",
    "TraceFile",
    "TraceKind",
    "TraceRecord",
    "get_route_detail_name",
]

@typing.final
class MoveKind:
    r"""
    Standard move-type classification shared by all operations.
    
    Every toolpath point is tagged with one of these so that renderers
    can colour and categorise moves generically.
    """
    CUT: MoveKind = MoveKind.CUT
    TRAVEL: MoveKind = MoveKind.TRAVEL
    PLUNGE: MoveKind = MoveKind.PLUNGE
    LEAD_IN: MoveKind = MoveKind.LEAD_IN
    LEAD_OUT: MoveKind = MoveKind.LEAD_OUT
    LINK: MoveKind = MoveKind.LINK
    RESUME: MoveKind = MoveKind.RESUME
    ROUTE: MoveKind = MoveKind.ROUTE
    @property
    def value(self) -> builtins.int: ...
    @property
    def name(self) -> builtins.str: ...
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class ResumeSource:
    r"""
    Resume-strategy enum for trace records.
    """
    NONE: ResumeSource = ...
    WALL_HUG: ResumeSource = ...
    SEGMENT: ResumeSource = ...
    MAT: ResumeSource = ...
    FRONTIER: ResumeSource = ...
    ENVELOPE: ResumeSource = ...
    ISLAND: ResumeSource = ...
    @property
    def value(self) -> builtins.int: ...
    @property
    def name(self) -> builtins.str: ...
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class RouteSource:
    r"""
    Route-strategy enum for trace records.
    """
    NONE: RouteSource = ...
    DIRECT: RouteSource = ...
    FRONTIER: RouteSource = ...
    MAT: RouteSource = ...
    ZHOP: RouteSource = ...
    @property
    def value(self) -> builtins.int: ...
    @property
    def name(self) -> builtins.str: ...
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class StepStatus:
    r"""
    Step-status enum for trace records.
    """
    OK: StepStatus = ...
    BOUNDARY_HIT: StepStatus = ...
    LOST_ENGAGEMENT: StepStatus = ...
    NO_CONVERGENCE: StepStatus = ...
    @property
    def value(self) -> builtins.int: ...
    @property
    def name(self) -> builtins.str: ...
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class TraceFile:
    r"""
    Binary trace file with random access to records.
    
    Usage::
    
        >>> from raygeo.trace import TraceFile
        >>> t = TraceFile("path/to/trace.bin")
        >>> len(t)          # number of records
        >>> t[0]            # first record (TraceRecord with dot access)
        >>> t.toolpath      # list of (x, y, move_kind) tuples
        >>> t.geometry      # dict with tool_radius, boundary, islands, seeds
        >>> t.mat_nodes     # MAT nodes or empty list
    """
    @property
    def ver(self) -> builtins.int: ...
    @property
    def geometry(self) -> dict:
        r"""
        Decoded geometry dict (tool_radius, boundary, islands, seeds) from the
        first record with ``kind == "geometry"``, or an empty dict.
        """
    @property
    def toolpath(self) -> builtins.list[tuple[builtins.float, builtins.float, builtins.int]]:
        r"""
        Toolpath points extracted from all motion records (those with
        ``pos_x`` / ``pos_y``).  Returns a list of ``(x, y, move_kind)``
        where ``move_kind`` is a ``MoveKind`` value.
        """
    @property
    def mat_nodes(self) -> builtins.list[tuple[builtins.float, builtins.float]]:
        r"""
        MAT nodes from the first record with ``kind == "mat"``, or an
        empty list.
        """
    @property
    def mat_clearances(self) -> builtins.list[builtins.float]: ...
    @property
    def mat_edges(self) -> builtins.list[tuple[builtins.int, builtins.int]]: ...
    @property
    def mat_root(self) -> builtins.int: ...
    def __new__(cls, path: builtins.str) -> TraceFile: ...
    def __len__(self) -> builtins.int: ...
    def __getitem__(self, idx: builtins.int) -> TraceRecord: ...

@typing.final
class TraceKind:
    r"""
    Record-kind enum for trace events.
    """
    INIT: TraceKind = ...
    CUT: TraceKind = ...
    RESUME_STALL: TraceKind = ...
    RESUME_STUCK: TraceKind = ...
    EXIT: TraceKind = ...
    @property
    def value(self) -> builtins.int: ...
    @property
    def name(self) -> builtins.str: ...
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class TraceRecord:
    r"""
    One per-step trace record.
    
    Wraps the decoded msgpack map and exposes fields as attributes.
    Supports both dot access (``rec.kind``) and dict access (``rec["kind"]``).
    """
    def __new__(cls, d: dict) -> TraceRecord: ...
    def __getattr__(self, name: builtins.str) -> typing.Any: ...
    def __getitem__(self, name: builtins.str) -> typing.Any: ...
    def __repr__(self) -> builtins.str: ...
    def __contains__(self, name: builtins.str) -> builtins.bool: ...
    def keys(self) -> builtins.list[builtins.str]: ...

def get_route_detail_name(detail: int) -> str:
    r"""
    Return a human-readable label for a route-strategy detail code.
    """

